user: {
    username: String,
    password: String,
    name: String,
    email: String,
    pollsCreated: [poll_id]
}

poll: {
    author: user_id,
    question: String,
    options: [{
        body: String,
        votes: Number
    }],
    voters: [user_id],
    replies: [reply_id],
    category: String,
    pollType: String,
    isPollOpen: Boolean,
    isDiscussionOpen: Boolean
}

reply: {
   sender: user_id,
   body: String,
   replies: [reply_id]
}